#include <stdio.h>
/**
 * main - this programm
 *
 * Description: this programm
 * fdfdfdf
 * Return: 0
 */
int main(void)
{
	char ch

	for (ch = 'a' ; ch <= 'z' ; char++)
	{
		putchar (ch);
	}
	for (ch = 'A' ; ch <= 'A' ; char++)
	{
		putchar (ch);
	}
	putchar ('\n'):
	return (0);
}
